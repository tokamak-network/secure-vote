# 🚀 빠른 시작 가이드

## 방법 1: 웹 UI 데모 (추천)

**한 줄 명령으로 모든 것 실행:**

```bash
./start-demo.sh
```

**실행 내용:**
- ✅ Anvil 로컬 블록체인 시작
- ✅ 스마트 컨트랙트 자동 배포
- ✅ Frontend 설정 및 서버 시작
- ✅ 브라우저 자동 오픈 (http://localhost:3000)

**이후 할 일:**

1. **MetaMask 설정** (최초 1회만):
   - 네트워크 추가: Anvil (http://127.0.0.1:8545, chainId: 31337)
   - 계정 가져오기: `0x7c852118294e51e653712a81e05800f419141751be58f605c371e15141b007a6`

2. **웹에서 테스트**:
   - "Setup Demo" 클릭
   - "Connect Wallet" 클릭
   - "Vote" → Yes/No 선택
   - "Committee" → "Decrypt & Tally" 클릭
   - 결과 확인!

**종료:**
- `Ctrl+C` 누르면 모든 프로세스 자동 종료

---

## 방법 2: 완전 자동 테스트 (MetaMask 불필요)

**터미널에서만 전체 플로우 테스트:**

```bash
./auto-test.sh
```

**실행 내용:**
- ✅ Anvil 시작
- ✅ 컨트랙트 배포
- ✅ Threshold 키 생성 (3/5)
- ✅ 제안 생성
- ✅ 3개 투표 제출 (암호화)
- ✅ Threshold 복호화
- ✅ 집계 및 온체인 제출
- ✅ Merkle 증명 검증
- ✅ 최종 결과 출력

**장점:**
- MetaMask 설정 불필요
- 브라우저 필요 없음
- 완전 자동화된 E2E 테스트
- CI/CD에 바로 사용 가능

**소요 시간:** 약 30초

---

## 비교

| 항목 | start-demo.sh | auto-test.sh |
|------|---------------|--------------|
| UI | 웹 브라우저 | 터미널 |
| MetaMask | 필요 (1회 설정) | 불필요 |
| 인터랙션 | 수동 클릭 | 완전 자동 |
| 용도 | 데모/사용자 테스트 | 개발/CI 테스트 |
| 시간 | 2-3분 | 30초 |

---

## 수동 실행 (옛날 방법)

여전히 수동으로 하고 싶다면:

```bash
# 터미널 1
anvil

# 터미널 2
forge script script/Deploy.s.sol --rpc-url http://127.0.0.1:8545 --broadcast --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# 터미널 3
cd frontend
echo "NEXT_PUBLIC_CONTRACT_ADDRESS=0x..." > .env.local
npm run dev
```

---

## 문제 해결

### "Permission denied" 에러
```bash
chmod +x start-demo.sh auto-test.sh
```

### 포트가 이미 사용 중
```bash
pkill -f anvil
pkill -f "next dev"
```

### 처음부터 다시 시작
```bash
# 모든 프로세스 종료
pkill -f anvil
pkill -f "next dev"

# 다시 실행
./start-demo.sh
```

---

## 팁

### 로그 확인
```bash
# Anvil 로그
tail -f /tmp/anvil.log

# Next.js 로그
tail -f /tmp/nextjs.log
```

### 빠른 재시작
`start-demo.sh`는 Ctrl+C로 종료 후 다시 실행하면 모든 것이 재설정됩니다.

### MetaMask는 한 번만 설정
네트워크와 계정은 한 번 설정하면 계속 사용할 수 있습니다.

---

## 다음 단계

1. ✅ 로컬 테스트 완료
2. 📝 코드 수정 및 실험
3. 🌐 Sepolia 테스트넷 배포
4. 🎨 UI 커스터마이징
5. 🔐 실제 키 관리 구현

---

## 도움말

- 웹 UI 문서: `frontend/README.md`
- 테스트 가이드: `frontend/TEST.md`
- 구현 상세: `frontend/IMPLEMENTATION.md`
- 전체 문서: `FRONTEND.md`
